import { NavSliderDirective } from './nav-slider.directive';

describe('NavSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new NavSliderDirective();
    expect(directive).toBeTruthy();
  });
});
